function repeatString (number, str) {
    return str.repeat(number);
    // newString = "";
    // for (let i = 0; i<number; i++) {
    //     newString += str;
    // }
    // return newString
}
console.log(repeatString(3," sliman "))